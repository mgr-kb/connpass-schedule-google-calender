import { createComponent } from "@vue/composition-api";

export default createComponent({
  name: "About",
  setup() {
    return () => (
      <div class="about">
        <h1>Googleカレンダー連携(準備中)</h1>
      </div>
    );
  }
});
